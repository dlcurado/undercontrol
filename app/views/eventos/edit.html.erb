<section class="wrapper site-min-height">
	<h3><i class="fa fa-angle-right"></i> Editar informações da festa</h3>
	<div class="row mt">
		<div class="col-lg-12">
			<%= render 'menu_completo' %>
			
			<%= render 'form' %>
		</div>
	</div>

</section><! --/wrapper -->
